sudo: required
services:
  - docker

before_install:
  - docker build -t willchang6471/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true willchang6471/docker-react npm run test -- --coverage


